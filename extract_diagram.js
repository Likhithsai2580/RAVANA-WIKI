// Test extracting the mermaid diagram from our file\nconst fs = require('fs');\nconst content = fs.readFileSync('docs/Action System/Action Exceptions.md', 'utf-8');\n\n// Find the mermaid diagram\nconst start = content.indexOf('```mermaid');\nconst end = content.indexOf('```', start + 10);\n\nif (start !== -1 && end !== -1) {\n  const diagram = content.substring(start, end + 3);\n  console.log(\"Found diagram:\");\n  console.log(diagram);\n  \n  // Extract just the content\n  const diagramContent = content.substring(start + 10, end).trim();\n  console.log(\"\\nDiagram content:\");\n  console.log(diagramContent);\n} else {\n  console.log(\"No diagram found\");\n}